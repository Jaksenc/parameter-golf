# Bridge v15: locate the learning bottleneck before adaptation

Primary: on event questions, compare numeric_mass with legacy_codes when both receive the EXACT SAME ordinary model-generated draft. Co-primary descriptive measurements are target TVD and sum((p-q)^2), with strict log loss, false zeros, mode identification and measured computation always reported. No score-selected replacement. No training or Jev API calls in this diagnostic.

Population: 24 new generated worlds, six each of counts, selected-stratum conditioning, two-stage mixtures and Bayesian conditioning. Two to five mutually exclusive labels; includes zero-probability outcomes and unique modes. Each world has an event-law and a determinate modal-answer query (48 tasks). Mode point targets are ground truth for a determinate question, not proof of model self-knowledge. The same world pairs are the resampling unit. These are four authored families, not an independently authored benchmark or full JevBench.

For every task generate one ordinary480 draft with the unchanged v7 ordinary-reasoning prompt. Preserve the draft, including incorrect or unfinished drafts. Then measure BOTH readouts under four supports: no note; that model draft; a privileged correct selection of relevant raw operands with an operation description but without normalized target probabilities; a privileged exact probability calculation. All supports retain original evidence. Both channels get identical notes, and support/channel order is randomized deterministically. The selected-evidence intervention includes representation simplification and procedural hints; it cannot uniquely identify evidence retrieval as the cause of a gain. The oracle condition tests fidelity, not autonomous accuracy. Only no-note and model-draft paths are autonomous.

Both readouts are unchanged from Measure v14: legacy restricted-code softmax and a 96-token greedy numerical weight array followed by normalization. A malformed numerical response uses the same-view legacy vector and remains in all denominators. Real numerical estimates and token-softmax confidence have different origins, labeled explicitly. No smoothing, temperature fitting, removal of wrong drafts, family-specific route, or oracle repair. Scores use exact Fraction targets. Report strict infinite log losses whenever a positive target gets zero; epsilon-clipped loss is a separate diagnostic only.

Main work: 48 ordinary generations, 192 numeric generations, 192 code readouts, 16 native anchors and 16 projection checks. All pretrained weights and parent sources pinned to the existing Qwen3.5-4B experiment. Standard public GitHub CPU workers only, max16 parallel, no paid fallback; main unchanged. Tasks hash/schema-checked; records atomically flushed and fsynced per complete matched task. Failures are not omitted. Recover only unavailable complete task records, never by correctness.

Analysis: independently reconstruct laws from source text / finite tickets; replay numeric parsing with exact rational arithmetic and softmax separately; derive labels from reported vectors. Report all four support conditions and both requested quantities. Bootstrap10,000 paired worlds within four source strata for contrasts, with both question types kept linked; intervals conditional on this small generated set, no multiplicity or prior research correction. Timing includes ordinary generation when used plus actual output-channel work, without charging unused research controls. Reference computation is privileged, not billed as model reasoning. Report result disagreements and illustrative counterexamples without using correctness to repair any output.

Next gate: only after localization select a matched direct-training comparison. Supervised control must see the SAME independent examples and transformations as a relational-loss arm, with equal backbone, initialization, parameter count, and computational budget. Relevant edits, distractor invariance, renaming and partition relations require independently correct target maps. These current diagnostic worlds become regression-only, never held-out training validation. This protocol does not claim that new weights have already been trained or that consistency implies truth.

Source commit a32e6c0f972966e0b20fdc7ec1fb4c3f8f7ba747, tests commit2118fee2e7a65cc56e6b0ecd8e3018cfe5ad95a2. Parent Measure v14 artifact10724905763, run35796368589. Preflight CI must pass before inference. Generated inputs and scientific source hashes are saved in the prepare artifact before any new model output. No broad novelty priority, calibrated correctness guarantee or Jev-superiority claim.

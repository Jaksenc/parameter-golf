# Execution-only transport of Compact Evidence v0.1

The frozen package is copied unchanged onto this isolated compute branch after the user's request to run it. The prior package's descriptive local-only/no-remote-run policy fields are retained verbatim for protocol reproducibility; this file records the now-authorized execution exception. No main-branch mutation, benchmark submission, paid accelerator, external model API or training is authorized by this workflow. Standard public-repository ARM workers execute the pinned original model. Artifacts have short retention; storage billing is not independently inspected.

All scientific source files, prompts, cases and reference hashes must match the uploaded archive before inference. The two incomplete transport-draft files in earlier branch commits are omitted from this tree and are never executed. Development and replication use identical frozen model settings; no results select a new prompt or alter replication.

# Contrast v7 — frozen before new outcomes

Primary: contrast_consensus. A 320-token maximum original reasoning path supplies the initial answer, with the unchanged typed readout if no complete FINAL is available. A separate 160-token constraint-first path receives only the original task, not the initial answer or draft. If their answers differ, a categorical judge compares the same two proposals twice with reversed presentation order. Switch to the challenger only if both orderings select that semantic answer; otherwise retain the initial answer. Agreement is a heuristic, not a correctness certificate; the two judge passes are correlated, not independent trials.

Predeclared controls: native direct readout; original uninterrupted 320-token reasoning; original uninterrupted 480-token reasoning; blind challenger alone; single-order judge. All arms, including negative findings, must be reported. No threshold fitting, oracle routing, answer-dependent selection, or replacement of the primary based on benchmark results. A new uninterrupted 480-token trace supplies its actual first 320 token IDs, not a retokenized old trace. Generation stops normally at EOS. Prefix timings are observed from the same live decode. The longer 480 control and primary (320+160) have equal maximum generated-token ceilings, not equal actual tokens or FLOPs: separate prompts, readouts, and judges have explicit extra costs.

Population: all 231 old public JevBench decisions at the pinned snapshot, plus 64 independently authored BIG-Bench Hard examples (eight per listed family) selected before model output inspection. BBH commit 9ee07bd481feebf959a6b59d61ea57bdcf30964d; all source bytes verified by known Git blob hashes. Families: causal judgement, date understanding, disambiguation QA, formal fallacies, seven-object deduction, sports understanding, temporal sequences, and seven-object shuffled tracking. Seed 714203; deterministic random ordering followed by normalized input-stem exclusion against all previously used experiment tasks. Answer keys do not affect inclusion. BBH is public since 2022: these are new examples for this project's evaluation, not a pretraining-contamination-free or private holdout. Published input canaries are preserved in the source archive and never used for training.

Each public input is newly run in all applicable branches. No previous Resume answer is fed into the new solver. Its 200/231 history is independently reconstructed only for comparison. Model Qwen3.5-4B revision 851bf6e806efd8d0a36b00ddf55e13ccb7b8cd0a, unchanged frozen BF16 weights and FP32 categorical projection. Generated paths explicitly request last-token projection, logits_to_keep=1, to avoid unnecessary full-prefill vocabulary output; native anchors verify the unchanged direct path. Every shard verifies both pretrained weight hashes, a full/restricted readout fixture and a saved native anchor. Successful generation/control semantics must be distinguished from tests with injected models.

Main endpoint: paired public accuracy primary vs long320, and vs the stronger long480 control; external BBH provides a separate generalization diagnostic. Report all per-task labels, repairs/regressions, paired source-group bootstrap for public tasks and family-stratified item bootstrap for this finite external sample. Eight task families do not constitute a population-level universality guarantee. Intervals are exploratory and not multiplicity-adjusted. No calibrated probabilities are fabricated for label-preserving hybrid outputs; raw judge/readout distributions are uncalibrated. No full JevBench composite, current live ranking, hidden-set score, official submission, new weights or Jev API calls are claimed.

Scientific rationale: prior Resume cannot repair its preserved completed mistakes. Independent re-solving may expose missed constraints without anchoring to the initial answer. The order-consistent switch gate tests whether unreliable adjudication can be made less damaging. Compare against its single-order ablation and blind challenger to avoid attributing prompt effects or simple selection to a novel reasoning ability. Existing primary research includes Chain-of-Verification (2309.11495), intrinsic self-correction failures (2310.01798), and budget forcing (2501.19393). No literature-wide priority claim.

Local preflight: 35 tests passed; no new model outcomes inspected. Scientific source SHA256: 52771fdb5d8c7e5315c6f49ac87b725ba1244c0fe5ba4f5a0674ec4de3778d0b. Test source SHA256: 4996bc8173ac6c1bda16b6de036e3423aba5fdab06c61f8f57dd1ec94dbe450c. Main branch remains unchanged. All runtime errors and unfinished tasks must remain explicit, with recovery preserving complete original rows rather than cherry-picking successes.

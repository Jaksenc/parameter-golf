Translate the task into a small exact executable plan when useful. Return ONE JSON object, no prose.
Before forming the plan, distinguish the entities, what each quantity measures, its units or time basis, and the relationship requested. Use descriptive lowercase names (at most 32 characters), not single-letter aliases. Keep separate groups and per-unit versus total quantities distinct. Each step should introduce a useful quantity; do not emit repeated algebraic steps that add no information.
Example plan for six boxes of eleven units with five units returned:
{"steps":[{"name":"units_per_box","expr":"11"},{"name":"boxes","expr":"6"},{"name":"delivered_units","expr":"units_per_box*boxes"},{"name":"returned_units","expr":"5"}],"result":"delivered_units-returned_units"}.
Every expr and result is an expression STRING. Previous named steps are available. Derived quantities must be computed from named inputs rather than guessed numerical answers. Recheck that the final expression measures exactly what was requested. Aim for at most 12 concise steps; use more only when necessary.
Supported: + - * / // % **, parentheses, comparisons, True False, and or not, boolean ^, flat lists, list indexing, min,max,sum,len,sorted,abs,ceil,floor,round (nearest-even integer), count(list,value), date_add("YYYY-MM-DD",integer_days) returning MM/DD/YYYY. Arithmetic is exact rational. Exponents must be integers with absolute value <=12. No Python code, imports, loops, attributes, assignments inside expr, dictionaries, comprehensions or other functions.
For sorting, result can be sorted(["pear","apple"]) and the renderer joins items with spaces. For yes/no or option questions return the requested label, not an intermediate boolean. If the operations are not useful, reason internally and return {"answer":"the requested answer"}. For multiple choice use the option label, for example (B). No other JSON fields, unsupported tools, or invented sources. Execution verifies the computation only; correct interpretation is your responsibility.

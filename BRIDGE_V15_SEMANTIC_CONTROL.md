# Additional control, fixed before outcome inspection

The v14 distribution-aware code readout improved several proper-loss measurements at lower computation than numerical output. Comparing only numerical output against the legacy code prompt would omit that stronger simple control.

Without altering the running Bridge primary or its records, add the EXACT v14 semantic_codes readout to every one of the same 192 evidence/support views. Reuse the saved ordinary draft verbatim. No new draft, labels, targets, calibration, confidence gate, or numerical-output-dependent selection. Reconstruct notes from the frozen Bridge jobs and recorded drafts. Report semantic_codes beside the two primary channels; it is a secondary control, not a replacement primary.

This control is declared before any new Bridge model outputs are inspected. It requires 192 additional single-pass code readouts with the same pinned backbone/runtime and exact weight/anchor checks. Use standard free CPU workers only. Readout duration is retained separately; generation cost remains the same shared recorded draft cost. All 192 outputs and independent probability arithmetic must be present before calling this control complete.

The original co-primary comparison remains model_draft numerical versus legacy on event TVD and squared-vector error. No method will be selected for promotion from this tiny diagnostic alone.
